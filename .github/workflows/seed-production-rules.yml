name: Run aircraft production rules seed

on:
  workflow_dispatch:

jobs:
  seed-production-rules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Run production rules seed
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npm run seed:production-rules
